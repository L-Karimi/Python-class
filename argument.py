from ast import Num


def my_country(student ="Lucy",country="Kenya"):
        return print(f'Hello { student},you are from {country}.')
def sum_and_greet(*args,**kwargs):
        sum =0
        for sum in args:
                sum += num
                keys = keys.kwargs
        if "num" in keys() :
                print(f"Hello{kwargs['name']} the answer is {sum}")
elif 'country in keys':
        print(f"Hello {kwargs['name']} from {kwargs['country']} the answer is {sum}")
elif not kwargs:
        print(f'"Hello anounymous the answer is{sum}')
                        
                
                
                
                